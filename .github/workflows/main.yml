name: DevSecOps Chatroom CI/CD

on:
  push:
    branches: ["main"]

env:
  DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USERNAME }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push Auth Service
        run: |
          docker build -t $DOCKERHUB_USER/chatroom-auth-service:latest ./auth-service
          docker push $DOCKERHUB_USER/chatroom-auth-service:latest

      - name: Build & Push File Service
        run: |
          docker build -t $DOCKERHUB_USER/chatroom-file-service:latest ./file-service
          docker push $DOCKERHUB_USER/chatroom-file-service:latest

      - name: Build & Push Backend
        run: |
          docker build -t $DOCKERHUB_USER/chatroom-backend:latest ./backend
          docker push $DOCKERHUB_USER/chatroom-backend:latest

      - name: Build & Push Frontend
        run: |
          docker build -t $DOCKERHUB_USER/chatroom-frontend:latest ./frontend
          docker push $DOCKERHUB_USER/chatroom-frontend:latest


  deploy:
    needs: build-and-push
    runs-on: self-hosted

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Pull latest Docker images
        run: docker compose pull

      - name: Restart services
        run: docker compose up -d --remove-orphans
